---
import Button from "../all-pages/button.astro";
import VideoCard from "../all-pages/video-card.astro";
const { title, path, pdf } = Astro.props;
---

<section class="grid lg:grid-cols-2 sm:grid-cols-1 border-4 border-primary-500 rounded-lg p-8 lg:gap-8 mb-8">
  <div class=" flex flex-col justify-center gap-4">
    <h2 class=" text-center lg:text-2xl md:text-xl sm:text-lg font-bold text-primary-500">{title}</h2>
    <p>
      <slot />
    </p>
    <!----<Button link={pdf} isActive></Button> -->
  </div>
  <VideoCard title={"Presentación Electiva: " + title} path={path} />
</section>
